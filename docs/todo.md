* set the `initSpace` call to use our own server & lambda functions.
  We call this in the main `state.ts` file, client side
  
  Need to implement
    - `/api/create-space`
    - `/api/space-exists`
